++ add changable server
++ using optparser
++ using a privmsg write function
++ anser to more irc command like ping
++ change $VERSION$ and $HOME$ PHANTOM_VERSION=None and XBRAIN_VERSION=None in all file